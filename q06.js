//Crie uma função que use a função de array map para transformar uma lista de números em uma lista de seus quadrados.

const funcao = (numeros) => numeros.map(elemento => elemento * elemento);

console.log(funcao([1,2,3]))